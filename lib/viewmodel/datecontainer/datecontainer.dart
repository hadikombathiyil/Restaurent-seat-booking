import 'package:flutter/material.dart';

class Datecontainer extends StatelessWidget {
  const Datecontainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: 70,
      height: 30,
      decoration: BoxDecoration(color: Color.fromARGB(255, 2, 73, 86)),
    );
  }
}
